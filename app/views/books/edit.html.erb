<h1>Editing Book</h1>

<%= form_with model: @book, url: book_path(@book.id), meethod: :patch do |f| %>
  <p>Title</br>
  <%= f.text_field :title %></br></p>
  <p>Body</br>
  <%= f.text_area :body %></br></p>
  <%= f.submit 'Update Book' %>
<% end %>

<%= link_to "Show", book_path(book.id) %>
|
<%= link_to "Back", books_path %>